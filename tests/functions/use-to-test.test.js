/* global describe, it, expect */

const { functionUsedToTest } = require('../../functions/use-to-test');

describe('', () => {
  it('', () => {
    const event = functionUsedToTest({ name: 'event' });
    expect(event).toBe(event);
  });
});
